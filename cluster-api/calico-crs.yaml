apiVersion: addons.cluster.x-k8s.io/v1beta1
kind: ClusterResourceSet
metadata:
  name: calico-crs
  namespace: genesis
spec:
  clusterSelector:
    matchLabels:
      cni: calico
  resources:
    - name: calico-cni
      kind: ConfigMap
  strategy: ApplyOnce
