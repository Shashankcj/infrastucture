apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: bootstrap-register-binding
subjects:
  - kind: ServiceAccount
    name: bootstrap-register-sa
    namespace: genesis
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: bootstrap-register-role